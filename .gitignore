node_modules/
dist/
.env
*.env.local
.DS_Store

# Agoda hotel enrichment data — lives on the Render persistent disk in prod,
# and locally you should symlink/copy it in for dev rather than commit it
# (agoda_hotels.sqlite is ~390MB). See NOTES-agoda-hosting.md.
agoda_hotels.sqlite
agoda_city_lookup.json

# Retail product catalog (Shopee/iHerb) — same reasoning, see product-db.ts and
# product-refresh.ts. Auto-rebuilt in production; not committed. The .meta.json
# sidecar tracks the last refresh timestamp/row counts (see product-refresh.ts).
products.sqlite
products.sqlite.meta.json
